name = "cashflow"
pages_build_output_dir = "dist"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# D1 database binding — configured in Cloudflare Dashboard (Settings → Functions → D1 Database Bindings)
[[d1_databases]]
binding = "cashflow"
database_name = "cashflow"

# PASSWORD is configured as a secret environment variable in Cloudflare Dashboard
